# ==============================================
# HogoDog Frontend Terraform Environment Variables
# ==============================================
# このファイルをコピーして .env.terraform として使用してください
# cp .env.terraform.example .env.terraform

# ====================
# Terraform Variables (terraform.tfvarsの上書き)
# ====================

# プロジェクト基本設定
export TF_VAR_project_name="hogo-dog"

# ドメイン設定
export TF_VAR_domain_name="hogodog.jp"

# AWSリージョン設定
export TF_VAR_aws_region="ap-northeast-1"

# ====================
# AWS Credentials (AWS CLIが設定済みの場合は不要)
# ====================

# export AWS_PROFILE="your-aws-profile"
# export AWS_ACCESS_KEY_ID="your-access-key"
# export AWS_SECRET_ACCESS_KEY="your-secret-key"

# ====================
# 使用方法
# ====================

echo "✅ Terraform environment variables loaded!"
echo ""
echo "🚀 Basic usage:"
echo "  cd terraform/environments/dev"
echo "  terraform init -backend-config=\"bucket=your-state-bucket\" \\"
echo "                 -backend-config=\"key=hogo-dog-frontend/dev/terraform.tfstate\" \\"
echo "                 -backend-config=\"region=ap-northeast-1\""
echo "  terraform plan -var-file=\"terraform.tfvars\""
echo "  terraform apply -var-file=\"terraform.tfvars\""
echo ""
echo "📝 Don't forget to:"
echo "  1. Create terraform.tfvars from terraform.tfvars.example"
echo "  2. Set your actual domain name and S3 backend bucket"