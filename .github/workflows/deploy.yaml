# This file is deprecated - use deploy-dev.yaml or deploy-prod.yaml instead
# Legacy deployment workflow for backward compatibility

name: Legacy Deploy (Deprecated)

on:
  # Disabled - use deploy-dev.yaml or deploy-prod.yaml instead
  workflow_dispatch:

jobs:
  legacy-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Deprecation Notice
      run: |
        echo "⚠️ This workflow is deprecated"
        echo "Please use one of the following workflows instead:"
        echo "  - deploy-dev.yaml for development deployments"
        echo "  - deploy-prod.yaml for production deployments"
        echo ""
        echo "GitHub Actions → Actions tab → Select appropriate workflow"
        exit 1